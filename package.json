{
  "name": "yogs",
  "version": "1.0.0",
  "description": "Jingle Jam 2018 interactive schedule",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist/",
    "build:apache": "mkdir -p dist && cp .htaccess dist/",
    "build:parcel": "parcel build index.html --public-url ./",
    "test": "npm-run-all clean --parallel build:*",
    "start": "parcel index.html"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ryanoconr/yogs.git"
  },
  "keywords": [
    "calendar"
  ],
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/ryanoconr/yogs/issues"
  },
  "homepage": "https://yogs.ryanoconr.com/",
  "devDependencies": {
    "cssnano": "^4.1.7",
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.6.2",
    "sass": "^1.15.2"
  }
}
